name: theme_editor
description: Flutter web app for editing and previewing the ThemeData object.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  analyzer:

flutter:
  uses-material-design: true
